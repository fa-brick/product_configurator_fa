# ⓘ Aucun modèle : ce pont est entièrement côté client, plus une vue. Odoo donne déjà
# `product_tmpl_id` sur un ordre de fabrication, et `product_configurator_fa_mrp` donne
# `config_ok` et `config_session_id` — il ne restait qu'à brancher le geste.
from . import tests
