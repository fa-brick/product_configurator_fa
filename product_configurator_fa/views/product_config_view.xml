<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Configuration Steps -->
    <record id="config_step_form_view" model="ir.ui.view">
        <field name="name">product.configurator.config.step.form</field>
        <field name="model">product.config.step</field>
        <field name="arch" type="xml">
            <form string="Configuration Step">
                <sheet>
                    <group>
                        <group>
                            <field name="name" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="config_step_tree_view" model="ir.ui.view">
        <field name="name">product.configurator.config.step.tree</field>
        <field name="model">product.config.step</field>
        <field name="arch" type="xml">
            <form string="Configuration Step">
                <list name="name">
                    <field name="name" />
                </list>
            </form>
        </field>
    </record>

    <!-- Configuration Restrictions -->
    <record id="product_config_domain_form_view" model="ir.ui.view">
        <field name="name">product.configurator.domain.form</field>
        <field name="model">product.config.domain</field>
        <field name="arch" type="xml">
            <form string="Configuration Restrictions">
                <group>
                    <group>
                        <field name="name" />
                    </group>
                </group>
                <notebook>
                    <page string="Rules">
                        <field name="domain_line_ids">
                            <list editable="bottom">
                                <field name="sequence" widget="handle" />
                                <field
                                    name="attribute_id"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                />
                                <field name="condition" />
                                <field
                                    name="template_attribute_value_ids"
                                    column_invisible="1"
                                />
                                <field name="attribute_value_type" column_invisible="1" />
                                <field
                                    name="value_ids"
                                    widget="many2many_tags"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                    context="{'show_attribute': False}"
                                    domain="[('id', 'in', template_attribute_value_ids)]"
                                    invisible="product_ids"
                                />
                                <!-- C4, D-201 : la seconde forme de feuille. Visible
                                     seulement sur un attribut de type produit, et elle
                                     efface l'autre — une ligne désigne l'un OU l'autre. -->
                                <field
                                    name="product_ids"
                                    widget="many2many_tags"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                    invisible="attribute_value_type != 'product'"
                                />
                                <field name="operator" />
                            </list>
                        </field>
                    </page>
                    <page string="Inherited">
                        <field name="implied_ids" />
                    </page>
                </notebook>
            </form>
        </field>
    </record>

    <!-- Created seprate view for the template -->
    <record id="product_config_domain_form_view_template" model="ir.ui.view">
        <field name="name">product.configurator.domain.form.template</field>
        <field name="model">product.config.domain</field>
        <field name="arch" type="xml">
            <form string="Configuration Restrictions">
                <group>
                    <group>
                        <field name="name" />
                    </group>
                </group>
                <notebook>
                    <page string="Rules">
                        <field name="domain_line_ids">
                            <list editable="bottom">
                                <field name="sequence" widget="handle" />

                                <field
                                    name="attribute_id"
                                    domain="[('id', 'in', context.get('product_attribute_ids', []))]"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                />
                                <field name="condition" />
                                <field
                                    name="template_attribute_value_ids"
                                    column_invisible="1"
                                />
                                <field name="attribute_value_type" column_invisible="1" />
                                <field
                                    name="value_ids"
                                    widget="many2many_tags"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                    context="{'show_attribute': False}"
                                    domain="[('id', 'in', template_attribute_value_ids)]"
                                    invisible="product_ids"
                                />
                                <!-- C4, D-201 : la seconde forme de feuille. Visible
                                     seulement sur un attribut de type produit, et elle
                                     efface l'autre — une ligne désigne l'un OU l'autre. -->
                                <field
                                    name="product_ids"
                                    widget="many2many_tags"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                    invisible="attribute_value_type != 'product'"
                                />
                                <field name="operator" />
                            </list>
                        </field>
                    </page>
                    <page string="Inherited">
                        <field name="implied_ids" />
                    </page>
                </notebook>
            </form>
        </field>
    </record>

    <record id="product_config_session_tree_view" model="ir.ui.view">
        <field name="name">product.config.session.tree</field>
        <field name="model">product.config.session</field>
        <field name="arch" type="xml">
            <list create="false">
                <field name="name" />
                <field name="product_tmpl_id" />
                <field name="value_ids" widget="many2many_tags" />
                <field name="user_id" />
                <field name="custom_value_ids" />
                <field name="currency_id" invisible="1" />
                <field
                    name="price"
                    widget='monetary'
                    options="{'currency_field': 'currency_id', 'field_digits': True}"
                />
                <field name="config_step_name" />
                <field name="product_id" />
                <field name="state" />
            </list>
        </field>
    </record>

    <record id="product_config_session_form_view" model="ir.ui.view">
        <field name="name">product.config.session.form</field>
        <field name="model">product.config.session</field>
        <field name="arch" type="xml">
            <form string="Configuration Sessions" editable="bottom" create="false">
                <header>
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name" nolabel="1" />
                        </h1>
                    </group>
                    <group>
                        <field name="product_tmpl_id" />
                        <field name="user_id" />
                        <field name="value_ids" widget="many2many_tags" />
                        <field name="currency_id" invisible="1" />
                        <field
                            name="price"
                            widget='monetary'
                            options="{'currency_field': 'currency_id', 'field_digits': True}"
                        />
                        <field name="config_step_name" />
                        <field name="product_id" />
                    </group>
                    <notebook>
                        <page name="custom_vals" string="Custom Values">
                            <field name="custom_value_ids">
                                <list editable="bottom">
                                    <field name="attribute_id" />
                                    <field name="value" />
                                    <field
                                        name="attachment_ids"
                                        widget="many2many_tags"
                                    />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <!-- ⚠️ LES RÉGLAGES D'UNE ÉTAPE, en FORMULAIRE — D-213.
         L'arbre a remplacé la section « Configuration Steps », qui portait l'ordre
         et l'appartenance (désormais déduits, D-202) MAIS aussi la condition de
         visibilité et la vue 3D. Sans ce formulaire, les retirer perdrait les
         deux dernières. -->
    <record id="product_config_step_line_form" model="ir.ui.view">
        <field name="name">product.config.step.line.form</field>
        <field name="model">product.config.step.line</field>
        <field name="arch" type="xml">
            <form string="Configuration step">
                <group>
                    <field name="config_step_id" readonly="1" />
                    <!-- ⓘ En lecture : l'appartenance vient de l'ORDRE des lignes
                         d'attribut, et se règle dans l'arbre (D-202). -->
                    <field name="attribute_line_ids" widget="many2many_tags" readonly="1" />
                    <field name="required" readonly="1" widget="boolean_toggle" />
                    <field
                        name="visibility_domain_id"
                        context="{'product_tmpl_id': context.get('product_tmpl_id'), 'form_view_ref': 'product_configurator_fa.product_config_domain_form_view_template'}"
                    />
                </group>
            </form>
        </field>
    </record>

</odoo>
